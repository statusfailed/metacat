{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "npm: compile metacat-vscode",
      "type": "shell",
      "command": "npm run compile",
      "options": {
        "cwd": "${workspaceFolder}/metacat-vscode"
      },
      "group": "build",
      "problemMatcher": "$tsc"
    }
  ]
}
