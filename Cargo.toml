[package]
name = "metacat"
version = "0.1.0"
edition = "2024"

[dependencies]
hexpr = "0.2.0"
open-hypergraphs = "0.2.9"
open-hypergraphs-dot = { version = "0.2.3", features = ["svg"] }

[dev-dependencies]
anyhow = "1.0.100"
